/**
 * This package contains the controller of the application.<br>
 * It contains everything that is needed to run the application, the logic behind the program.<br>
 * There are various classes that are called classNameManager.java, these classes have a specific
 * task to do (e.g. the {@link controller.TimerManager timerManager} class just updates the UI timer).
 * @author Patrich Tivoli
 */
package controller;